# Render deployment

Create a Static Site from this repository.

- Build command: `npm run build`
- Publish directory: `dist`

After deployment, use the HTTPS URL to package SmartScan AI for Microsoft Store.
